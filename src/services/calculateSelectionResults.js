

const calculateSelectionResults = (ref) => {

  const dom_object = ref.current;

  const current_select =  window.getSelection();


  // TODO: count how many characters, words, and sentances are in the
  // current selection and return it here
}

export default calculateSelectionResults;